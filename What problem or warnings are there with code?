Syntax Error in 

useState() : const [setSelectedIndex, selectedIndex] = useState(); setSelectedIndex should be 2nd params because value store in setSelected will update our state. 
const[selectedIndex,setSelectedIndex]=useState(); this is the correct syntax.


Type Error in

shape() function shapeOf() is not any function
WrappedListComponent.propTypes = {
  items: PropTypes.arrayOf(
    PropTypes.shape({
      text: PropTypes.string.isRequired,
    })
  ),
};
